{
  "name": "print-service-site",
  "private": true,
  "main": "index.js",
  "description": "3D print service — frontend + Express backend",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "node scripts/install-server-deps.js",
    "build": "node scripts/install-server-deps.js",
    "start": "node index.js",
    "dev": "npm --prefix server run dev",
    "setup": "npm install --prefix server"
  }
}
